<div class="card dashboard-card border-0 shadow-sm overflow-hidden">
    <!-- Card Header with Gradient -->
    <div class="card-header bg-gradient-primary py-3 border-0">
        <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-0 fw-bold text-white">
                <i class="fas fa-calendar-alt me-2"></i>Calendrier des Projets
            </h5>
            <div class="dropdown">
                <button class="btn btn-sm btn-light rounded-circle" type="button" data-bs-toggle="dropdown">
                    <i class="fas fa-ellipsis-v"></i>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><a class="dropdown-item" href="#"><i class="fas fa-plus me-2"></i>Ajouter un événement</a></li>
                    <li><a class="dropdown-item" href="#"><i class="fas fa-download me-2"></i>Exporter</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#"><i class="fas fa-cog me-2"></i>Paramètres</a></li>
                </ul>
            </div>
        </div>
    </div>
    
    <!-- Calendar Body -->
    <div class="card-body p-0">
        <!-- Quick Actions Bar -->
        <div class="bg-light border-bottom p-3">
            <div class="d-flex flex-wrap gap-2 justify-content-between align-items-center">
                <div class="btn-group btn-group-sm" role="group">
                    <button type="button" class="btn btn-outline-primary" id="calendar-today">
                        <i class="fas fa-calendar-day me-1"></i>Aujourd'hui
                    </button>
                    <button type="button" class="btn btn-outline-primary" id="calendar-prev">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <button type="button" class="btn btn-outline-primary" id="calendar-next">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
                
                <h6 class="mb-0 fw-semibold" id="calendar-month-year">mars 2026</h6>
                
                <div class="btn-group btn-group-sm" role="group">
                    <button type="button" class="btn btn-outline-primary active" data-view="monthGrid">Mois</button>
                    <button type="button" class="btn btn-outline-primary" data-view="timeGridWeek">Semaine</button>
                    <button type="button" class="btn btn-outline-primary" data-view="timeGridDay">Jour</button>
                </div>
            </div>
        </div>
        
        <!-- Calendar Container -->
        <div id="project-calendar" class="calendar-wrapper p-3" style="min-height: 350px;">
            <!-- FullCalendar will be initialized here -->
            <div class="text-center py-5">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">Chargement...</span>
                </div>
                <p class="mt-2 text-muted mb-0">Chargement du calendrier...</p>
            </div>
        </div>
    </div>
    
    <!-- Card Footer with Upcoming Events -->
    <div class="card-footer bg-white border-top-0 px-3 py-2">
        <div class="d-flex justify-content-between align-items-center">
            <small class="text-muted">
                <i class="fas fa-calendar-week me-1"></i>
                <span id="event-count">0</span> événements ce mois
            </small>
            <a href="#" class="text-decoration-none small">
                Voir tout <i class="fas fa-arrow-right ms-1"></i>
            </a>
        </div>
    </div>
</div>

<style>
/* Custom Calendar Styling */
.dashboard-card {
    transition: transform 0.2s, box-shadow 0.2s;
    border-radius: 1rem;
}

.dashboard-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
}

.bg-gradient-primary {
    background: linear-gradient(135deg, #0d6efd 0%, #0a58ca 100%);
    border-radius: 1rem 1rem 0 0;
}

.calendar-wrapper {
    background: #ffffff;
}

/* FullCalendar Custom Overrides */
.fc {
    font-family: inherit;
}

.fc .fc-toolbar-title {
    font-size: 1rem;
    font-weight: 600;
}

.fc .fc-button-primary {
    background-color: #f8f9fa;
    border-color: #dee2e6;
    color: #212529;
    text-transform: capitalize;
    font-weight: 500;
}

.fc .fc-button-primary:hover {
    background-color: #e9ecef;
    border-color: #dee2e6;
    color: #0d6efd;
}

.fc .fc-button-primary:focus {
    box-shadow: none;
}

.fc .fc-button-primary:disabled {
    background-color: #f8f9fa;
    border-color: #dee2e6;
    opacity: 0.5;
}

.fc .fc-button-primary.fc-button-active {
    background-color: #0d6efd;
    border-color: #0d6efd;
    color: white;
}

.fc .fc-daygrid-day.fc-day-today {
    background-color: rgba(13, 110, 253, 0.05);
}

.fc .fc-daygrid-day-number {
    font-size: 0.875rem;
    font-weight: 500;
    padding: 0.5rem;
}

.fc .fc-col-header-cell-cushion {
    padding: 0.75rem 0.5rem;
    font-weight: 600;
    text-decoration: none;
    color: #6c757d;
}

.fc .fc-daygrid-day-events {
    min-height: 2rem;
}

/* Event styling */
.fc-event {
    background-color: #0d6efd;
    border: none;
    border-radius: 0.25rem;
    padding: 0.125rem 0.25rem;
    font-size: 0.75rem;
    cursor: pointer;
    transition: transform 0.1s;
}

.fc-event:hover {
    transform: scale(1.02);
    filter: brightness(0.95);
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .fc .fc-toolbar {
        flex-direction: column;
        gap: 0.5rem;
    }
    
    .fc .fc-toolbar-chunk {
        display: flex;
        justify-content: center;
        width: 100%;
    }
    
    .fc .fc-daygrid-day-number {
        font-size: 0.75rem;
        padding: 0.25rem;
    }
    
    .btn-group-sm .btn {
        padding: 0.25rem 0.5rem;
        font-size: 0.75rem;
    }
}

/* Dark mode support */
@media (prefers-color-scheme: dark) {
    .bg-gradient-primary {
        background: linear-gradient(135deg, #0b5ed7 0%, #084298 100%);
    }
    
    .calendar-wrapper {
        background: #212529;
    }
    
    .fc .fc-daygrid-day {
        background-color: #212529;
        border-color: #2c3034;
    }
    
    .fc .fc-daygrid-day-number {
        color: #f8f9fa;
    }
    
    .fc .fc-col-header-cell-cushion {
        color: #adb5bd;
    }
    
    .card-footer {
        background-color: #212529 !important;
    }
}
</style>

<script>
// Enhanced calendar initialization script
document.addEventListener('DOMContentLoaded', function() {
    var calendarEl = document.getElementById('project-calendar');
    
    if (calendarEl) {
        var calendar = new FullCalendar.Calendar(calendarEl, {
            initialView: 'dayGridMonth',
            locale: 'fr',
            firstDay: 1, // Monday first day of week
            headerToolbar: false, // We're using custom toolbar
            height: 'auto',
            contentHeight: 'auto',
            aspectRatio: 1.5,
            events: [
                // Example events - replace with your dynamic data
                {
                    title: 'Réunion équipe',
                    start: '2026-03-15',
                    color: '#0d6efd'
                },
                {
                    title: 'Deadline projet',
                    start: '2026-03-25',
                    end: '2026-03-26',
                    color: '#dc3545'
                },
                {
                    title: 'Formation',
                    start: '2026-03-20T10:00:00',
                    color: '#198754'
                }
            ],
            eventClick: function(info) {
                // Handle event click
                alert('Événement: ' + info.event.title);
                // You can open a modal with event details here
            },
            dateClick: function(info) {
                // Handle date click - add new event
                console.log('Date clicked: ' + info.dateStr);
                // You can open a modal to add event here
            },
            dayCellDidMount: function(info) {
                // Add custom tooltip or styling for specific dates
                if (info.date.toDateString() === new Date().toDateString()) {
                    info.el.style.backgroundColor = 'rgba(13, 110, 253, 0.05)';
                }
            }
        });
        
        calendar.render();
        
        // Update event count
        function updateEventCount() {
            var events = calendar.getEvents();
            document.getElementById('event-count').textContent = events.length;
        }
        
        // Custom toolbar controls
        document.getElementById('calendar-today')?.addEventListener('click', function() {
            calendar.today();
            updateMonthYear();
        });
        
        document.getElementById('calendar-prev')?.addEventListener('click', function() {
            calendar.prev();
            updateMonthYear();
        });
        
        document.getElementById('calendar-next')?.addEventListener('click', function() {
            calendar.next();
            updateMonthYear();
        });
        
        // View switching
        document.querySelectorAll('[data-view]').forEach(btn => {
            btn.addEventListener('click', function() {
                const view = this.getAttribute('data-view');
                calendar.changeView(view);
                
                // Update active state
                document.querySelectorAll('[data-view]').forEach(b => {
                    b.classList.remove('active');
                });
                this.classList.add('active');
                
                updateMonthYear();
            });
        });
        
        function updateMonthYear() {
            const currentDate = calendar.getDate();
            const monthNames = ['janvier', 'février', 'mars', 'avril', 'mai', 'juin', 
                               'juillet', 'août', 'septembre', 'octobre', 'novembre', 'décembre'];
            const monthYear = `${monthNames[currentDate.getMonth()]} ${currentDate.getFullYear()}`;
            document.getElementById('calendar-month-year').textContent = monthYear;
        }
        
        // Initial update
        calendar.on('datesSet', updateMonthYear);
        calendar.on('eventAdd', updateEventCount);
        calendar.on('eventRemove', updateEventCount);
        
        updateMonthYear();
        updateEventCount();
    }
});
</script>




Make sure you have FullCalendar CSS and JS included in your project:

html
<link href="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/locales/fr.js"></script>
